<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--配置自定义reaml-->
    <bean id="userRealm" class="com.zju.sms.shiro.UserRealm"/>
    <bean id="agentRealm" class="com.zju.sms.shiro.AgentRealm"/>
    <bean id="adminRealm" class="com.zju.sms.shiro.AdminRealm"/>

    <!--配置自定义的认证器，可以针对不同的登陆类型使用不同的Realm-->
    <bean id="authenticator" class="com.zju.sms.shiro.MyModularRealmAuthenticator">
        <!--认证策略，一个Realm认证成功即可，并返回认证信息-->
        <property name="authenticationStrategy">
            <bean class="org.apache.shiro.authc.pam.AtLeastOneSuccessfulStrategy"/>
        </property>
    </bean>

    <!--配置安全管理器SecurityManager-->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <!--配置认证器-->
        <property name="authenticator" ref="authenticator"/>
        <!--配置多个Realm-->
        <property name="realms">
            <list>
                <ref bean="userRealm"/>
                <ref bean="agentRealm"/>
                <ref bean="adminRealm"/>
            </list>
        </property>
    </bean>

    <!--定义shiroFilter-->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <property name="securityManager" ref="securityManager"/>
        <property name="loginUrl" value="/login"/>
        <property name="unauthorizedUrl" value="/nopermission.jsp"/>
        <property name="filterChainDefinitions">
            <value>
                /**=anon
               <!-- /login=anon
                /login/**=anon
                /**=authc-->
            </value>
        </property>
    </bean>

    <!--配置授权-->
    <!--开启aop，对类代理-->
    <aop:config proxy-target-class="true"/>
    <!--开启shiro注解支持（@RequiredPermissions）-->
    <bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">
        <property name="securityManager" ref="securityManager"/>
    </bean>

    <!--权限异常处理-->
    <bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
        <property name="exceptionMappings">
            <props>
                <prop key="UnauthoriziedException">redirect:/nopermission.jsp</prop>
            </props>
        </property>
    </bean>


</beans>
